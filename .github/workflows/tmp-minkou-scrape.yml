name: Temporary Minkou Scrape
on:
  push:
    branches:
      - tmp-minkou-scrape-20260829
  workflow_dispatch:

permissions:
  contents: read

jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install dependencies
        run: pip install requests beautifulsoup4 lxml
      - name: Scrape Minkou public pages
        run: python scripts/minkou_scrape.py
      - name: Show manifest
        if: always()
        run: |
          cat minkou_output/manifest.json || true
          cat minkou_output/errors.json || true
      - name: Upload results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: minkou-all-data
          path: minkou_output/
          retention-days: 1
